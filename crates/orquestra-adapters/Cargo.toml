[package]
name = "orquestra-adapters"
version = "0.1.0"
edition = "2024"
license.workspace = true
repository.workspace = true

[dependencies]
orquestra-core = { path = "../orquestra-core" }
which = "7"
serde = { version = "1", features = ["derive"] }
tracing = "0.1"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
